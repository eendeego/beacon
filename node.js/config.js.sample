// Copy this file to config.js and adjust URLs.
// For other settings check the beacon.js file.

module.exports = {
  services : [
    { name: 'hudson', url: 'http://hudson' }
  ],

  beaconUrl : 'http://beacon',

  updateInterval : 1000,

  verbose : true,
};
